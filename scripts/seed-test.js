const mongoose = require('mongoose');

// Adjust the path to correctly require compiled TypeScript files or simply
// use ts-node to run this script if you keep it as TS. For simplicity, we'll write this
// as a TypeScript file and instruct to use ts-node.
